# Post 001 Review Instructions

Use this dry run to validate the Orion Effects LinkedIn human-in-loop process.

1. Open GitHub Actions.
2. Run `LinkedIn Human Approval Manual`.
3. Enter the review issue number when available.
4. Choose `approve` to mark the content ready for manual publishing.
5. Do not publish externally during this dry run.
6. Run the workflow again with `published` only when performing a real publication test.

The comment-driven `/approve` workflow remains available, but the manual-dispatch workflow is the reliable fallback if repository event execution is blocked.
